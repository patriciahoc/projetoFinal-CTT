{
  "devDependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.0.2"
  },
  "scripts": {
    "start": "json-server db.json -m ./node_modules/json-server-auth -r routes.json --port 4000"
  }
}
